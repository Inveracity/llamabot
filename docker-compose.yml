services:
  ollama-small:
    image: ollama/ollama:latest
    container_name: ollama-small
    volumes:
      - ./ollama_data:/root/.ollama
      - ./ollama-entrypoint.sh:/entrypoint.sh
    restart: unless-stopped
    environment:
      - OLLAMA_NUM_PARALLEL=1
      - OLLAMA_MAX_LOADED_MODELS=1
      - OLLAMA_MAX_QUEUE=1
      - MODEL_NAME=${MODEL_NAME:-qwen3.5:0.8b}
    entrypoint: ["/bin/bash", "/entrypoint.sh"]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  llama-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llama-bot
    volumes:
      - ./system_prompt.md:/app/system_prompt.md:ro
      - ./chad_prompt.md:/app/chad_prompt.md:ro
    depends_on:
      ollama-small:
        condition: service_healthy
    env_file:
      - .env
    environment:
      - OLLAMA_HOST=http://ollama-small:11434
      - MODEL_NAME=${MODEL_NAME:-qwen3.5:0.8b}
    restart: unless-stopped
